unit Unit11;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, ExtCtrls, DBCtrls, StdCtrls, Grids, DBGrids, jpeg, Buttons;

type
  TTestTopic = class(TForm)
    Image1: TImage;
    DBGrid1: TDBGrid;
    DBNavigator1: TDBNavigator;
    BitBtn1: TBitBtn;
    procedure Button1Click(Sender: TObject);
    procedure BitBtn1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  TestTopic: TTestTopic;

implementation

uses
  Unit3, Unit9;

{$R *.dfm}

procedure TTestTopic.Button1Click(Sender: TObject);
begin
Quest.Showmodal;
end;

procedure TTestTopic.BitBtn1Click(Sender: TObject);
begin
Quest.Showmodal;
end;

end.
